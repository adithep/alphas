template(name="alpha_form")

  a(class="save_human_json") Save Json
  nav(class="form_nav")
    ul
      each schema_buttons
        +_schema_buttons

  form(class="contact_form")
    div
      each input_element
        +_each_input
    a(class="form_submit") Submit

template(name="_schema_buttons")
  li
    a(class="click-input") #{this._dis}

template(name="_string_input")
