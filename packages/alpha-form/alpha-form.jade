template(name="alpha_form")

  a(class="save_human_json right") Save Json
  nav(class="form_nav")
    ul
      each schema_buttons
        +_schema_buttons

  form(class="contact_form")
    each input_element
      +_each_input
    br
    a(class="form_submit") Submit

template(name="_schema_buttons")
  li
    a(class="click_input") #{this._dis}

template(name="_string_input")
  div(class="contact_div")
    span Î
    input(type="#{this.type}", placeholder="#{this._dis}", class="input_ui")
template(name="_string_select")
  div(class="contact_div")
    span Î
    select(class="input_ui")
      each select_options
        +_string_select_options
template(name="_string_select_options")
  option #{this._v}
